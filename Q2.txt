Q2:
关于推荐系统，之前几乎一无所知，也是这两天翻看了一些基础知识。

1. 传统推荐算法：如协同滤波。由于移动互联网数据量非常大，通过搜索全部用户寻找与目标用户相似度排名较高的用户会极大的影响推荐系统的效率，所以，在收集到大量用户数据后，预先对后台用户数据进行聚类。 然后再进行协同滤波，比如使用基于领域的算法，求出P(u, i)即用户u对商品i的感兴趣程度，对于商品i，选取对其最感兴趣的前k个用户。

2. 机器学习模型：把所有的用户、商品、行为数据都建模成特征，是否购买建模成label，建模成一个回归模型。